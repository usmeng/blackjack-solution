package util;
/*package main;

import java.util.Random;

import bean.Card;

public class GameCard {

	private Card[] cards;
	
	public GameCard(int number){
		initDeckCard(number);
	}
	
	public Card[] getCards() {
		return cards;
	}

	public void setCards(Card[] cards) {
		this.cards = cards;
	}
	
	public Card deal(int position) {
		if (position >= 0 && position < cards.length)
			return cards[position];
		return null;
	}

	public void initDeckCard(int index){
		if(index <= 0) return;
		cards = new Card[DeckCard.DECK_LENGTH * index];
		for (int i = 0; i < index; i++) {
			DeckCard deck = DeckCard.newInstance();
			Card[] cs = deck.getCards();
			for (int j = 0; j < cs.length; j++) {
				cards[i * DeckCard.DECK_LENGTH + j] = cs[j];
			}
		}
	}
	
	public void shuffle(){
		Random random = new Random();
		for (int i = 0; i < cards.length; i++) {
			int value = random.nextInt(cards.length - i) + i;
			Card temp = cards[value];
			cards[value] = cards[i];
			cards[i] = temp;
		}
	}
	
	public void print(){
		for (int i = 0; i < cards.length; i++) {
			Card card = cards[i];
			System.out.println(i + 1 + ": " + card.toString());
		}
	}
	
	public static void main(String[] args) {
		GameCard card = new GameCard(8);
		card.shuffle();
		card.print();
	}
}
*/